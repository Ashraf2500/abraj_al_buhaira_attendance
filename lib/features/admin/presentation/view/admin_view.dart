import 'package:flutter/material.dart';

import 'admin_view_exports.dart';

class AdminView extends StatelessWidget {
  const AdminView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AdminBody();
  }
}
